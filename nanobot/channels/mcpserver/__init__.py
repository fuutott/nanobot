"""MCP Server channel package (fork).

Thin in-tree wrapper that makes the fork's ``nanobot-channel-mcpserver`` plugin
discoverable under upstream's self-contained channel model. The runtime class
ships in the installed plugin package; ``runtime.py`` re-exports it.
"""
