@AGENTS.md

## Local scratch: todo.md

There is a git-ignored `todo.md` at the repo root holding fork
divergence-reduction tasks (e.g. leaning on upstream's native custom-provider
support and channel dependency management to shrink the fork delta). It is
local-only — not committed, not pushed. Check it when picking up fork-maintenance
work; update it as items are done or new divergence-reduction candidates appear.
